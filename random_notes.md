# Random notes

Taken because I felt they may be usefull for later, or just per interest... just learning


### Virtual environment

`pip install virtualenv`
```bash
# create and select new virtual env
virtualenv venv_name
source venv_name/bin/activate

# quit virtual env
deactivate
```


### OpenCV & Scikit-image

`pip install opencv-contrib-python` or [from source code](https://www.pyimagesearch.com/opencv-tutorials-resources-guides)

`pip install scikit-image`